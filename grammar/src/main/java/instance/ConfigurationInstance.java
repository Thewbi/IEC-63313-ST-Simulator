package instance;

// import java.util.HashMap;
// import java.util.Map;

public class ConfigurationInstance extends VariableInstance {

    // private Map<String, VariableInstance> variables = new HashMap<>();

    // public void addVariable(VariableInstance variableInstance) {
    //     if (variables.containsKey(variableInstance.getName())) {
    //         throw new RuntimeException("Variable " + variableInstance.getName() + " contained already!");
    //     }
    //     variables.put(variableInstance.getName(), variableInstance);
    // }
    
}
